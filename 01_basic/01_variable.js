const name="kartik Mahato"
let age=28;
var title="Mahato"
sal=2.0;

//
//name="Rohit"  // we can't re-assign constant variable
age=29
title="singh"
sal=22;

/* We not use var variable in js because it will not fallow scope it can update 
or re-assign form any where on same Js file */

console.log(name)
console.log(age)
console.log(title)
console.log(sal)
